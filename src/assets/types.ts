export interface wordAndTranslate {
    word: string,
    translate: string
}

export interface fromTo {
    from: number,
    to: number
}

export interface styleType {
    transform: string
    transition: string
    zIndex?: number
    opacity?: number
}